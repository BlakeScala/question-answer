<div class="well questionTiles">
  {{#link-to 'question' question.id}}<h3>{{question.mainQuestion}}</h3>{{/link-to}}
  <h5>Asked by {{question.author}}</h5>
  {{#if added}}
  {{else}}
    <button id="star" {{action 'starThis' question}}></button>
  {{/if}}
</div>
